# Handoff — 2026-06-30

## 한 줄 요약
LangGraph로 ReAct 도구-사용 에이전트를 **밑바닥부터(StateGraph 직접 조립)** 구현하는 학습 중. 공식 Quickstart를 따라 첫 동작 버전을 완성했고, 다음은 여기에 RAG retriever를 도구로 붙이고 evals 관점으로 평가하는 단계.

## 컨텍스트 (왜 이걸 하고 있나)
이 저장소(`/Users/jangdaehan/llm-wiki`)는 Obsidian 기반 LLM 학습 위키. `raw/` 아래에 날짜별 학습 노트/코드가 쌓임. 학습 궤적은 두 갈래:
- **밑바닥 이해**: micrograd → torch char-NLP → attention 구현 → 트랜스포머 책 → CPython 소스 추적
- **LLM 앱/에이전트**: LangChain → LangGraph(counter 단계) → function calling → vector RAG → graph RAG(Neo4j) → Anthropic 에이전트 에세이 5편

직전 세션에서 "이론(에세이 5편)은 많이 읽었는데 에이전트 루프를 직접 안 짜봤다"는 공백을 진단 → **LangGraph ReAct 도구-사용 에이전트 만들기**를 다음 1시간 학습으로 추천함.

## 오늘 한 일
- 추천 학습 리소스 정리 (공식 Quickstart / Workflows&agents 문서 / DataCamp / swmansion / LangChain Academy)
- **LangGraph 공식 Quickstart**(docs.langchain.com/oss/python/langgraph/quickstart)를 따라 ReAct 루프 첫 구현 완료
  - 파일: `raw/light study/langchain_quickstart.py` (git 미추적 상태 `??`)
  - 구성: `@tool`(multiply/add/divide) + `bind_tools` + `MessagesState`(messages는 `operator.add` reducer, `llm_calls` 카운터) + `llm_call`/`tool_node` 두 노드 + `should_continue` 조건부 엣지로 `tool_node ↔ llm_call` 루프 + `END` 종료
  - 모델: `claude-haiku-4-5-20251001` (기존 langGraph 코드의 `openai:gpt-4o-mini`에서 바꿔 씀)
  - `ToolNode`/`tools_condition` 프리빌트 대신 **직접 손으로 조립**한 버전 — 학습 목적에 맞음

## 다음 할 일 (추천 1시간 플랜의 남은 단계)
1. **RAG retriever를 도구로 붙이기** — `raw/graph_rag_tutorial/2026_06_22/rag_vectorstore.py`의 retriever를 `@tool`로 감싸 에이전트에 연결. 산술 도구 자리에 검색 도구를 넣어 "검색할지/답할지"를 LLM이 판단하게.
2. **trajectory 관찰 + evals** — 질문 2~3개 던지고 `llm_calls` 카운터/도구 호출 횟수/정답 여부를 `raw/anthropic/Demystifying evals for AI agents.md` 관점에서 평가.
3. (선택) 학습 노트로 정리 → `raw/langGraph/2026-06-30-react-agent.md` 같은 형태로 커밋. 이 저장소는 front matter 기반 concept 지식맵을 쓰므로(커밋 d1f8f0d) 노트 작성 시 front matter 규칙 확인할 것.

## 참고 / 주의
- 직전 세션이 정리한 학습 리소스 링크(공식 Quickstart, Workflows&agents, DataCamp, swmansion, LangChain Academy intro-to-langgraph)는 대화 로그에 있음 — 필요하면 거기서 회수.
- `create_react_agent` 프리빌트 1줄 함수는 **지금은 쓰지 않기로** 함(루프를 손으로 짜보는 게 목표). 나중 단계용.
- 기존 `raw/langGraph/2026_06_21.py`는 counter StateGraph 예제(초기 단계). 새 파일은 `raw/light study/`에 들어가 있으니 위치 정리 여부는 사용자에게 확인.
- 커밋은 사용자가 요청할 때만. main 브랜치이므로 커밋 시 브랜치부터.

## Suggested skills
- 다음 세션에서 코드 검증이 필요하면: **verify** 또는 **run** (에이전트 실제 실행/관찰)
- RAG 도구 붙인 변경을 점검할 때: **code-review**
- LLM/모델 관련 질문(모델 id, tool use, caching 등)이 나오면: **claude-api**
